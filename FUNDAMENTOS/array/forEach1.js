const aprovados = [ 'Agatha', 'Aldo', 'Daniel', 'Raquel']

aprovados.forEach(function (nome, indice) {
    console.log(`${indice + 1}) ${nome}`)
})

/* 
.   A função que vai dentro do forEach tem como finalidade, atribuir aos parâmetros, 
(nome(nome do aprovado), indice(a colocacao de qual nome voce deseja percorrer))
forEach <- seria o laço For, percorre todos os elementos do array, signifca "Para cada"...
*/


