const [a, b, c, d] = [3, 5, 1, 15] // forma reduzida de criar 4 variáveis 

const soma = a + b + c + d
const subtracao = d - b 
const multiplicacao = a * b 
const divisao = d / a 
const modulo = a % 2
// modulo <- significa o resto da divisão 

console.log(soma, ",", subtracao, ",", multiplicacao, ",",  divisao, ",", modulo,".")